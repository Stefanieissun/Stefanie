let run = () => {
    if (!run.a) {
        run.a = 1
    }

    return (() => {
        run.a++;
        return run.a;
    })()
}


// 按值传递  引用传递  共享传递

// js中所有函数的参数的传递都是按值传递
// 按照引用传递是传递对象的引用   ,而共享传递是传递对象的引用的副本，复制了

